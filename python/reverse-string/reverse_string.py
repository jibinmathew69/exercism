def reverse(input=''):
    # length = len(input)
    # if length == 0:
    #     return input
    # half = length//2
    # length-=1
    # string = list(input)
    # for i in range(half):
    #     string[i] = input[length-i]
    #     string[length-i] = input[i]
    #
    # return ''.join(string)
    return input[::-1]